# 🪝 Custom Hooks

## `useGSAP.ts`

Custom hook for GSAP (GreenSock Animation Platform) integration with React.

Handles GSAP context creation and cleanup within React's lifecycle, preventing memory leaks and enabling scroll-triggered animations via ScrollTrigger plugin.
