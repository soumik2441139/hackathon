# 🔌 Configuration

Environment and infrastructure configuration.

| File | Purpose |
|------|---------|
| `env.ts` | Loads and validates environment variables via dotenv |
| `db.ts` | MongoDB connection setup with Mongoose |
| `cors.ts` | CORS origin allowlist configuration |
| `system.config.ts` | System-wide constants (rate limits, timeouts, etc.) |
